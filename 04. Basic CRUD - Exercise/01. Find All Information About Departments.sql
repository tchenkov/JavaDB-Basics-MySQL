#	01. Find All Information About Departments
#
# Write a SQL query to find all available information about the
# departments. Sort the information by id.

USE soft_uni;

SELECT *
FROM departments
ORDER BY department_id;